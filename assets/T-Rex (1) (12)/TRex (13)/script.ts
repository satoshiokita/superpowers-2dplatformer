// Solid. Arcade2d body property 'movable' is false.

class TRexBehavior extends Sup.Behavior {
  awake() {
    
  }

  update() {
    
    //Sup.ArcadePhysics2D.collides(this.actor.arcadeBody2D, this.solidBodies);
  }
}
Sup.registerBehavior(TRexBehavior);
